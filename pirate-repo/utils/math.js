function addGold(a,b){return a+b;}
// Math utility functions

const piratePass = "blackpearl123";

function distance(x1,y1,x2,y2){
    return Math.sqrt((x2-x1)**2 + (y2-y1)**2);
}

function addGold(a,b){ return a+b; }
function multiplyLoot(a,b){ return a*b; }

// Dummy loop
for(let i=0;i<50;i++){
    console.log(`Math calculation #${i}: ${addGold(i,i*2)}`);

}